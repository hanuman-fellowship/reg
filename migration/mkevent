drop table if exists event;
create table event (
    id          integer primary key autoincrement,
    name        text,
    descr       text,
    sdate       text,
    edate       text,
    sponsor     text,
    organization_id integer not null,
    max         text
);
